{
  "name": "zuudeui-packages",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "release": "changeset publish",
    "cropper:build": "pnpm --filter @workspace/cropper build",
    "ios-mockups:build": "pnpm --filter @workspace/ios-mockups build",
    "g": "tsx script.ts && prettier --write ./apps/ios-mockups/__registry__/index.ts"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.4",
    "@workspace/eslint-config": "workspace:*",
    "@workspace/typescript-config": "workspace:*",
    "prettier": "^3.5.1",
    "turbo": "^2.4.2",
    "typescript": "5.7.3"
  },
  "packageManager": "pnpm@10.4.1",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "tsx": "^4.19.4"
  }
}
